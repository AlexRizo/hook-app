import { useEffect } from "react"

export const useFetch = (url) => {
    useEffect(() => {
        const getFetch = async () => {
            
        }
    }, [url]);
    
  return (
    <div>useFetch</div>
  )
}